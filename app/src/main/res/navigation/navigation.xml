<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/placeHolder">
    <fragment
        android:id="@+id/placeHolder"
        android:name="fullerton.lfg.PlaceHolder"
        android:label="place_holder"
        tools:layout="@layout/place_holder" >
        <action
            android:id="@+id/action_placeHolder_to_userInfo3"
            app:destination="@id/userInfo" />
    </fragment>
    <fragment
        android:id="@+id/userInfo"
        android:name="fullerton.lfg.userInfo"
        android:label="user_info"
        tools:layout="@layout/user_info" />
</navigation>